# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Docker staging directory created by `make docker-ci`
docker-build/

# Test binary, built with `go test -c`
*.test
*.tmp

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# GoLand stuff
.idea/*

# build directories
bin/
dist/
cert/
asup/
site/