collector: ESeries

schedule:
  - counter: 24h
  - data: 3m

objects:
  Volume:            volume.yaml
  Array:             array.yaml
  Host:              host.yaml
  Controller:        controller.yaml
  Fan:               fan.yaml
  Battery:           battery.yaml
  PowerSupply:       power_supply.yaml
