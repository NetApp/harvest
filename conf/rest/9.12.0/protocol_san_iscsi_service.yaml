
name:             ProtocolSanIscsiService
query:            api/protocols/san/iscsi/services
object:           protocol_san_iscsi_service

counters:
  - ^^svm.uuid => svm_uuid
  - ^svm.name => svm
  - ^enabled   => enabled


plugins:
  - LabelAgent:
      value_to_num:
        - status enabled true true `0`

export_options:
  include_all_labels: true
