
name:             SecuritySSsh
query:            api/security/ssh
object:           security_ssh

counters:
  - ^^ciphers          => ciphers
  - ^mac_algorithms    => mac_algorithms
  - max_instances      => max_instances

export_options:
  instance_keys:
    - ciphers
  instance_labels:
    - mac_algorithms
