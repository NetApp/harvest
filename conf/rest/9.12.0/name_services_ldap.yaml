
name:             NameservicesLdap
query:            api/name-services/ldap
object:           name_services_ldap

counters:
  - ^^svm.uuid => svm_uuid
  - ^svm.name => svm
  - ^session_security   => session_security


plugins:
  - LabelAgent:
      value_to_num:
        - status session_security true true `0`

export_options:
  include_all_labels: true
