
name:             ClusterPeer
query:            api/cluster/peers
object:           cluster_peer

counters:
  - ^^uuid
  - ^encryption.proposed  => proposed

only_cluster_instance: true

plugins:
  - LabelAgent:
      value_to_num:
        - status proposed - - `0`

export_options:
  include_all_labels: true

# Need to test