
name:             Certificate
query:            api/security/certificates
object:           certificate

counters:
  - ^^uuid
  - ^expiry_time             => expiry_time
  - ^name
  - ^svm.name                => svm

export_options:
  instance_keys:
    - name
    - svm
    - uuid
  instance_labels:
    - expiry_time
