
name:             NtpServer
query:            api/cluster/ntp/servers
object:           ntpserver

counters:
  - ^^server => servers
#  - ^# => count

plugins:
  - LabelAgent:
      value_to_num:
        - statusc servers - - `0`

export_options:
  include_all_labels: true

# need testing for count