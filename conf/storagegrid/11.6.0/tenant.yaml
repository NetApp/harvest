
name:                       Tenant
query:                      grid/accounts-cache
object:                     tenant
api:                        v3

counters:
  - ^^id                    => id
  - ^name                   => tenant
  - objectCount             => objects
  - dataBytes               => logical_used
  - policy.quotaObjectBytes => logical_quota

plugins:
  - Tenant
  - Bucket

export_options:
  instance_keys:
    - id
  instance_labels:
    - tenant