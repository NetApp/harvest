# Offbox vscan counters from a per SVM perspective
name:                     VscanSVM
query:                    offbox_vscan
object:                   svm_vscan

instance_key:             uuid

counters:
  - connections_active
  - dispatch_latency
  - instance_name                  => svm
  - instance_uuid
  - scan_latency
  - scan_noti_received_rate
  - scan_request_dispatched_rate

export_options:
  instance_keys:
    - svm
