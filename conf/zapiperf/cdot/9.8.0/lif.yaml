
name:                     LIF
query:                    lif
object:                   lif

counters:
  - current_port             => port
  - instance_name
  - instance_uuid
  - node_name                => node
  - recv_data
  - recv_errors
  - recv_packet
  - sent_data
  - sent_errors
  - sent_packet
  - vserver_name             => svm

export_options:
  instance_keys:
    - lif
    - node
    - port
    - svm
