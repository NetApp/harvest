pipeline {
    agent any

	triggers {
        cron('@midnight')
    }

    parameters {
        string(name: 'VERSION', defaultValue: '', description: '[Optional] RPM Version should match Harvest version with rc or nightly stripped. If not filled default is YY.mm.ddHH. See https://github.com/NetApp/harvest-private/wiki/Release-Checklist#version-names for details')
		string(name: 'REVISION', defaultValue: '', description: '[Optional] Example: Nightly (default), GA See https://github.com/NetApp/harvest-private/wiki/Release-Checklist#rpm-and-debian-names for details ')
    }

    environment {
        key = credentials('HARVEST_ACCESS_TOKEN')
        VERSION =sh (returnStdout: true, script: """
        [ -n \"${params.VERSION}\" ] && echo \"${params.VERSION}\" || date +%Y.%m.%d%H | cut -c 3-
        """).trim()
		REVISION =sh (returnStdout: true, script: """
        [ -n \"${params.REVISION}\" ] && echo \"${params.REVISION}\" || echo "Nightly"
        """).trim()
        targetParentLocation = "/opt/home/nightly/"
    }

    stages {

        stage('clean workspace') {
            steps {
                cleanWs()
                sh 'rm -rf $targetParentLocation$VERSION$REVISION'
            }

        }
        stage('Setup GO') {
            steps {
                sh '''
                wget -O go.tar.gz "https://dl.google.com/go/$(curl https://golang.org/VERSION?m=text).linux-amd64.tar.gz"
                rm -rf /usr/local/go && tar -C /usr/local -xzf go.tar.gz
                '''
            }
        }

        stage('Download Prerequisites') {
            steps {
                sh '''
               apt-get update
               apt-get install -y rpm
               apt-get install -y net-tools
               apt install -y git-all
               apt-get install -y build-essential
                '''
            }
        }


        stage('Git Clone Harvest') {
            steps {
               sh '''
               git clone https://$key@github.com/NetApp/harvest-private.git
                '''
            }
        }

        stage('Build Harvest RPM/DEB') {
            steps {
               sh '''
               cd harvest-private
               targetLocation=$targetParentLocation$VERSION$REVISION
               rm -rf ./dist
               export PATH=$PATH:/usr/local/go/bin
               rm -rf /root/rpmbuild/
               mkdir -p $targetLocation
               ./package.sh deb x86_64 $VERSION $REVISION
               cp -RT ./dist/$VERSION-$REVISION/ $targetLocation
               ./package.sh rpm x86_64 $VERSION $REVISION
               cp -RT ./dist/$VERSION-$REVISION/x86_64/  $targetLocation
                '''
            }
        }

        stage('Build Harvest Tar gz') {
            steps {
               sh '''
               cd harvest-private
               export PATH=$PATH:/usr/local/go/bin
               targetLocation=$targetParentLocation$VERSION$REVISION
               rm -rf dist
               make dist-tar
               cp -RT ./dist/ $targetLocation
                '''
            }
        }

    }

    post {
        always {
            dir("$targetParentLocation$VERSION$REVISION") {
                archiveArtifacts artifacts: '**', fingerprint: true
            }
        }
    }
}