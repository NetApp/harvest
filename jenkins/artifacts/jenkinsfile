pipeline {
    agent any

    environment {
        key = credentials('HARVEST_ACCESS_TOKEN')
        harvestVersion = 2.0
        version =sh (returnStdout: true, script: 'echo $(date +"%Y%m%d%H00")').trim()
        targetParentLocation = "/opt/home/nightly/"
    }

    stages {

        stage('clean workspace') {
            steps {
                cleanWs()
                sh 'rm -rf $targetParentLocation$version'
            }

        }
        stage('Setup GO') {
            steps {
                sh '''
                wget -O go.tar.gz "https://dl.google.com/go/$(curl https://golang.org/VERSION?m=text).linux-amd64.tar.gz"
                rm -rf /usr/local/go && tar -C /usr/local -xzf go.tar.gz
                '''
            }
        }

        stage('Download Prerequisites') {
            steps {
                sh '''
               apt-get update
               apt-get install -y rpm
               apt-get install -y net-tools
               apt install -y git-all
               apt-get install -y build-essential
                '''
            }
        }


        stage('Git Clone Harvest') {
            steps {
               sh '''
               git clone https://$key@github.com/NetApp/harvest-private.git
                '''
            }
        }

        stage('Build Harvest RPM/DEB') {
            steps {
               sh '''
               cd harvest-private
               targetLocation=$targetParentLocation$version
               rm -rf ./dist
               export PATH=$PATH:/usr/local/go/bin
               rm -rf /root/rpmbuild/
               mkdir -p $targetLocation
               ./package.sh deb x86_64 $harvestVersion $version
               cp -RT ./dist/$harvestVersion-$version/ $targetLocation
               ./package.sh rpm x86_64 $harvestVersion $version
               cp -RT ./dist/$harvestVersion-$version/x86_64/  $targetLocation
                '''
            }
        }

        stage('Build Harvest Tar gz') {
            steps {
               sh '''
               cd harvest-private
               export PATH=$PATH:/usr/local/go/bin
               targetLocation=$targetParentLocation$version
               rm -rf dist
               make
               make dist-tar
               cp -RT ./dist/ $targetLocation
                '''
            }
        }

    }

    post {
        always {
            dir("$targetParentLocation$version") {
                archiveArtifacts artifacts: '**', fingerprint: true
            }
        }
    }
}