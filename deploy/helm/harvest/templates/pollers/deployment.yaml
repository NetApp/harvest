{{- range $name, $poller := .Values.harvestConfig.Pollers }}
{{- $fullDeploymentName := printf "%s-poller-%s" (include "harvest.fullname" $) $name -}}
{{- include "harvest.validations.DNS1123" $fullDeploymentName -}}
{{- $ctx := dict "root" $ "pollerName" $name "poller" $poller -}}
{{ include "harvest.poller.deployment" $ctx }}
---
{{ end }}
